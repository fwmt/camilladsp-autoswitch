[project]
name = "camilladsp-control"
version = "0.1.0"
description = "Safe automation and control layer for CamillaDSP"
requires-python = ">=3.9"

[project.scripts]
cdspctl = "camilladsp_control.cli:main"
